#include "FOCPA.h"

int main(int argv, char *argc[])
{
	if(argv != 2)
	{
		fprintf("stderr, syntax: LEA_CPA <option>\n\n");
		fprintf("option: 1 XOR key - XOR first byte\n";)
	}

	CPA();

	return 0;
}